// Define the TypeScript interface for an article
export interface Article {
  title: string;
  url: string;
  image: string;
  date: string;
  body: string;
  source: string;
  author: string;
}
